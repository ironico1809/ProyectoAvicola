"""Tests de la app `usuarios`.

Actualmente este archivo es un placeholder.
Si más adelante agregas tests, un buen punto de partida es cubrir:
- login (credenciales válidas/ inválidas),
- registro (hash de password),
- `/me/` con y sin token,
- asignación de roles (PUT/PATCH y validaciones de ids inexistentes).
"""

from django.test import TestCase
